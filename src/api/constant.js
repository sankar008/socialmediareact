export const CURRENCY = "$";

// ? BASE URL
const URL = "https://api.webdevelopments.in:3000/v1";

export const SIGNUP = `${URL}/user`;
export const USER = `${URL}/user`;
export const GROUPS = `${URL}/group`;
export const CATEGORYS = `${URL}/category`;
